target/
# The business database and what is written from it: rebuilt from AI/datasets on start
config/*.db
data/
# SolarHome's Python side: the venv, the unpacked app, exported datasets and trained models (large, rebuilt locally)
config/py/
# Secrets: copy .env.example to .env and fill it in
.env
# SolarFramework's mailer saves the mailbox, password included, here on first start
config/mail/
