target/
# The business database: rebuilt from config/py/mosaic/datasets on start
config/*.db
/data/
# config/py/mosaic is the Python project itself, tracked; its own .gitignore leaves out the venv and caches
# Secrets: copy .env.example to .env and fill it in
.env
# SolarFramework's mailer saves the mailbox, password included, here on first start
config/mail/
