# Test-wide overrides (classpath:config/ outranks the main application.properties): the application contexts under
# test never import the Olist CSVs or open config/Default.db.
mosaic.data.prepare-on-start=false
spring.datasource.url=jdbc:sqlite:target/test-context.db
