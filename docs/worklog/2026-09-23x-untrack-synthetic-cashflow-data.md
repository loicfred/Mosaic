# 2026-09-23 — Keep synthetic cash-flow data outside Git

Author: Codex

- Added `AI/datasets/small_business_cashflow.csv` to `AI/.gitignore` and removed it from Git tracking while preserving the local file. The supplied synthetic dataset has no recorded redistribution licence.
- Confirmed the API already treats this file as optional: observed cash-flow endpoints report unavailability when it is absent, and the model route returns HTTP 503.
- Verified `python -m pytest tests -q --tb=short --basetemp=.codex-test-tmp`: 161 passed in the final run, with two dependency deprecation warnings.
- The sales forecast API now selects the method with the lowest rolling backtest MAE; the investment suggestion uses the same method. With the committed Olist evaluation, that is the last-month rule (MAE BRL 54,998 versus BRL 92,045 for the trained model). The website names the selected method and avoids drawing its baseline twice.
- Verified `python -m app.main` served `/api/health` and `/openapi.json` with HTTP 200; `PagesRenderTest` passed; `node --check` passed for the changed chart script. The page was not checked in a real browser.- Kept the separate synthetic cash-flow profile as a backend-only demonstration. Its held-out ROC-AUC is 0.53, so ranking remains gated at HTTP 503; only observed sector/month stress rates are suitable to show. No website page was added for this separate dataset.
- Replaced the rough 3.4 BRL/USD display scale with 3.33, the equal-month mean of the Federal Reserve monthly averages for January 2017–August 2018 (https://fred.stlouisfed.org/data/EXBZUS). The footer links the source. Kept chart axes in BRL, matching the source transactions and avoiding a second approximate scale.
- Verified `FormatterTest` and `PagesRenderTest` after refreshing the local OpportunityImpl Maven artifact; the first website rerun failed on a stale `MosaicApi` class in that artifact.
- Documented the local canonical Olist orders and items SHA-256 hashes in `AI/README.md`, checked against the on-disk CSVs. Team-wide confirmation remains open in `docs/requirements.md`.
- Kept `BusinessDatabase` first-start import from `mosaic.data.source-dir` for the local demo. A fresh site has an empty SQLite database, and the existing import skips populated tables; assuming a prepopulated database would make first start depend on an unshipped artifact. Packaged startup remains open for a separate run.
- Built the executable website JAR and tested it in an isolated directory outside the repository. It initially failed before Spring startup because the archive omitted `slf4j-api`; Maven had resolved it as optional. Added an explicit nonoptional `slf4j-api` 2.0.18 dependency. The rebuilt JAR started Tomcat on port 18080 and imported the demo tables. Python autostart was disabled because an existing API occupied port 8000; this part remains open.
- A later `PagesRenderTest` rerun failed 8 assertions against concurrently changed overview/trend pages (old chart and route expectations), although it passed earlier in this session. The current failure is recorded in `docs/requirements.md`; the temporary smoke directory and copied data were removed.
- Added `/about` with the five authors supplied by the user, linked from the sidebar and footer. `AboutPageTest` first returned 404 before the route and passed after implementation.
- After the concurrent page work settled, the full OpportunityApp test suite passed (28 tests), including `AboutPageTest` and `PagesRenderTest`. Rebuilt the executable JAR to include `/about`.
