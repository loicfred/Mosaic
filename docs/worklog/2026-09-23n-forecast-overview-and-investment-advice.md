# 2026-09-23 — Forecast-only overview and investment advice when sales rise

Author: Claude

- Added `GET /api/sales/opportunities` (`AI/app/api/get_sales_opportunities.py`, logic in `AI/app/analysis/opportunities.py`). It compares the model's average forecast month with the last 3 observed months. Only when that rises does it list categories whose sales grew at least as fast as the business (with at least BRL 10,000 across both periods). Each category is checked against the business's late-delivery and low-review rates over the last 3 months, needing at least 30 orders for a verdict. It uses `require_model`, so an untrained model returns 503.
- Each candidate carries labels decided in code, with their thresholds in `rules`: `growth_level` (strong ≥ 20 pp, moderate ≥ 5 pp above the business), `size_level` (large ≥ 5 %, medium ≥ 1 % of recent sales), a `level` per rate (better/in_line/worse beyond ±2 pp), and `readiness` (ready/fix_first/unknown). The language model uses these labels as given and never re-judges them.
- On the real data, the forecast is +3.9 %. Health beauty, housewares, pet shop, construction tools and perfumery are growing, and all are in line with the business.
- Java: `MosaicApi.salesOpportunities`, `service/ai/InvestmentAdvisor.java` (the fixed template, plus the `InvestmentAdvisor` chatbot in `config/ai/agents.json`, with every number checked as in `ScenarioNarrator`), `controller/api/InvestmentAdviceController.java` (`/api/overview/advice`, fetched by the page after it loads), and a new assistant tool `MosaicToolbox.investmentOpportunities`.
- The overview (`index.html`) now shows only the sales forecast (`fragments/index/sales.html`) and the new `fragments/index/invest.html`. Removed the hidden-problem statement, the latest-month tiles and the "where to look next" links (`fragments/index/statement.html`, `latest.html`, `next.html`). Those screens are still in the sidebar.
- Tests: `AI/tests/test_opportunities.py` covers trend, filters, ranking, labels, the rate window, the missing model and flat sales. Also `InvestmentAdvisorTest`, and updates to `MosaicToolboxTest` and `PagesRenderTest`. Python: 92 passed. OpportunityImpl and OpportunityApp suites pass.
- Live AI check against LM Studio (gemma-4-e4b), using a temporary test class that was then deleted. The assistant answered where to invest, falling-behind categories, why sports_leisure is flagged, late deliveries vs reviews and the 20 % scenario, each in 55–130 s, with every figure verified. The least-reliable-sellers question timed out once and answered on retry (129 s, verified). The overview advice hit the 120 s timeout on both runs, so the page kept the template; the model has not yet written that advice live.
- Cloud AI: added a `Groq` service to `config/ai/agents.json` (`https://api.groq.com/openai`, `openai/gpt-oss-120b`). When the `GROQ_API_KEY` user environment variable is set, `LocalAi.useCloud` points every bot at it, so the key never enters the committed file. `LocalAi.reachable` treats an `https://` endpoint as up, because SolarFramework's `isAvailable()` probes LM Studio's own API. Without the variable, the site uses LM Studio as before. `LocalAiTest` covers both cases and no longer depends on the machine's environment.
- Live check on Groq: the overview advice (3.8 s) and the scenario summary were written by the model and passed the number check. The assistant answered where to invest, the forecast and the falling-behind categories in 4–6 s, all verified. The remaining questions, fired back-to-back, were refused by the free tier's limit of 8,000 tokens per minute (HTTP 429); the assistant showed its fallback message. The assistant ended two answers with invented page links (`/investmentOpportunities`, `/salesOverview`).
- Dollars: `Formatter.brl` now shows every amount as `BRL 863,390 (≈ USD 253,938)`, using `Formatter.BRL_PER_USD` = 3.4, a rough average over January 2017 to August 2018. The recorded reais stay the authoritative figure. The footer states the rate. The chart axes (`static/js/charts.js`) and the AI's own prose templates still show reais only.
- Simpler overview: the page is now the heading, the sales forecast chart and three buttons (`fragments/index/sales.html`). **Show evidence** opens the forecast table, the accuracy comparison, the forecast limitations and the data left out. **Suggest investment** fetches `/api/overview/advice`. **View possible caveats** fetches `/api/overview/caveats`. Both are served by `controller/api/OverviewAiController.java`, which replaces `InvestmentAdviceController`. Removed `fragments/index/invest.html`. When the API is down, the page shows the error under the heading.
- Caveats: `GET /api/sales/caveats` (`AI/app/analysis/caveats.py`) runs four checks. They cover categories falling behind the business, late-delivery and low-review rates over the last 3 months against the 3 before (pooled counts, triggered at +1 pp), and how much more often late orders get a 1–2 star review (triggered at 2×, or when on-time orders never get one). On the real data two checks trigger: 18 of 74 categories fell behind (BRL 461,022 less sales), and late orders get low reviews 62.4% of the time against 9.2% on time. Late and low-review rates fell (−6.5 pp and −5.0 pp), and the page says recent months may still have orders in transit. `service/ai/CaveatWriter.java` turns the checks into lines, which the `CaveatWriter` chatbot may rewrite. `service/ai/CheckedWriter.java` now holds the shared model-call-and-number-check that `InvestmentAdvisor` also uses.
- Tests: `AI/tests/test_caveats.py`, `CaveatWriterTest`, and the updated `PagesRenderTest`. Python 98 passed; both Java suites pass. The page itself was not opened in a browser, because the running site on port 8080 predates these changes.
